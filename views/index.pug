html
    head
        title= name
    body
        h1= name
        div.form.
        form(action='http://localhost:3000/auth/google', method='GET')
            input(type='submit', value='Zaloguj przez Google')